framework:
    rate_limiter:
        register_account:
            policy: 'token_bucket'
            limit: 1
            rate: { interval: '1 hour', amount: 1 }
        refresh_token:
            policy: 'token_bucket'
            limit: 1
            rate: { interval: '1 hour', amount: 1 }
